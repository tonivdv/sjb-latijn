"use client";

export default function Home({ params }: { params: { series: string } }) {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-3 text-xs">
      <h1 className="text-4xl">Binnenkort Beschikbaar !!</h1>
    </main>
  );
}
